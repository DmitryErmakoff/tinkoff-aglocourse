// Задача про наибольшой квадрат
package main

import (
	"bufio"
	"fmt"
	"os"
)

var (
	in  = bufio.NewReader(os.Stdin)
	out = bufio.NewWriter(os.Stdout)
)

func main() {
	var n, m int
	fmt.Fscanln(in, &n, &m)

	f
}
